#include <stdio.h>

struct P{
    int coeff;
    int deg;
    struct P* next;
};

void insertion(struct P* root, int c, int d){
    if(root==NULL){
        struct
    }
}

int main(){
    struct P* travel = NULL;
    int flash =0,c,d;
    while(flash!=-1){
        printf("Coeff: ");
        scanf("%d", &c);
        printf("Deg: ");
        scanf("%d", &d);

        printf("Continue? ");
        scanf("%d", &flash);
    }

}
